## Phase 1 — Baselining, data model & API (no UI changes yet)

- [x] 1.1 **Baseline `snowball-dev`** (this project has never used Prisma migrations — `prisma/migrations` doesn't exist and neither database has migration history; skipping this risks `prisma migrate dev` treating the live schema as drifted and offering to reset the database): create `prisma/migrations/0_init/`, generate its contents via `prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations/0_init/migration.sql`, then run `npx prisma migrate resolve --applied 0_init` against `snowball-dev` (confirm `.env`'s `DATABASE_URL` points at `snowball-dev` first); verify with `npx prisma migrate status` that it reports `0_init` applied and nothing pending, with no drift
- [x] 1.2 **Baseline production**: run `npx prisma migrate resolve --applied 0_init` against the production `Snowball` database, using the production connection string set locally and temporarily for this one command only — a deliberate manual step, never scripted or committed, performed once before any deploy that includes a migration step; verify with `npx prisma migrate status` (pointed at production) that it reports `0_init` applied and nothing pending, with no drift
- [x] 1.3 Add `pageConfigDraft Json?` to the `Campaign` model in `prisma/schema.prisma`; run `npx prisma migrate dev --name add_page_config_draft` against `snowball-dev` only — safe now that both databases are baselined (1.1, 1.2); verify the generated migration file only adds one nullable column with no data transformation
- [x] 1.4 Add `PageDraftEnvelopeSchema` (`{ headline: string | null, description: string | null, endsAt: coerced Date | null, pageConfig: PageConfigSchema }`, `.strict()`) and `parseDraftEnvelope(raw: unknown): PageDraftEnvelope | null` to `src/lib/pageConfig.ts` — returns `null` for `null`/invalid input (never a default envelope, unlike `parsePageConfig`); verify with a script covering null, valid, and malformed input
- [x] 1.5 Extend `PATCH /api/campaigns/:id`'s Zod schema with an optional `pageConfigDraft` field validated against `PageDraftEnvelopeSchema`, including the ENTRY_FORM-presence check applied to `pageConfigDraft.pageConfig`; verify: a valid `pageConfigDraft` is accepted and persisted, one missing an ENTRY_FORM section is rejected (400), and all previously-accepted fields (`status`, `headline`, `description`, `endsAt`, `pageConfig`, etc.) still work unchanged
- [x] 1.6 Add `POST /api/campaigns/:id/publish`: owner-only (404 for non-owners/unauthenticated, matching the existing pattern), 400 "No draft to publish" if `pageConfigDraft` is null, otherwise copies the draft's `headline`/`description`/`endsAt`/`pageConfig` into the campaign's published columns and clears `pageConfigDraft`, in one update; verify all three cases with direct requests
- [x] 1.7 Add `POST /api/campaigns/:id/discard-draft`: owner-only, clears `pageConfigDraft` to `null` unconditionally (200 whether or not one existed); verify both cases
- [x] 1.8 Add a `"vercel-build": "prisma migrate deploy && next build"` script to `package.json`; verify it isn't run locally by this task (only confirm the script text and, optionally, that `prisma migrate deploy` reports up to date when run locally against the now-baselined `snowball-dev`)
- [x] 1.9 Checkpoint: run `npm run typecheck`, `npm run build`, and `npm run test:e2e` and confirm all 12 existing flows still pass — nothing user-visible has changed yet

## Phase 2 — Editor shell restructure

- [x] 2.1 Update `src/app/dashboard/campaigns/[id]/edit/page.tsx` to compute the editor's initial content as `parseDraftEnvelope(campaign.pageConfigDraft) ?? { headline: campaign.headline, description: campaign.description, endsAt: campaign.endsAt, pageConfig: parsePageConfig(campaign.pageConfig) }`, and pass a `hasDraft: boolean` (`campaign.pageConfigDraft !== null`) to the editor
- [x] 2.2 Rebuild `PageBuilderEditor.tsx`'s layout into three zones: a top bar (campaign name, link back to `/dashboard/campaigns/:id`, device toggle, unpublished-changes indicator, Publish button), a left sidebar (section list), and a centre canvas (preview), replacing the current two-column layout
- [x] 2.3 Wire autosave with a single shared debounce timer (~800ms after the last edit of any kind, across all fields — not one timer per field, and not one request per keystroke): any text-field edit (headline, subtext, image URL) or structural edit (add/remove/reorder a section, change the countdown end date) resets the same timer, which then `PATCH`es the complete current draft to `pageConfigDraft`; update `hasDraft` from each successful response
- [x] 2.4 Build the top bar's save-state indicator as one evolving control cycling through idle (no draft) / saving (request in flight) / unpublished changes (last save succeeded) / failed to save; on failure, keep the owner's in-progress edits exactly as typed (never revert or clear them), offer an explicit Retry action, and confirm that the next edit also naturally retries the failed content (since each autosave sends the full draft, not a diff); add a `beforeunload` warning that fires while the indicator is in the saving or failed state
- [x] 2.5 Wire the top bar's Publish button to `POST /api/campaigns/:id/publish`, and a Discard control (visible only when `hasDraft` is true) to `POST /api/campaigns/:id/discard-draft`; both re-sync the editor's state from a fresh server round-trip on success and surface the server's error on failure
- [x] 2.6 Add the desktop/mobile device toggle to the top bar; the canvas container renders at full width for desktop and ~390px for mobile, with no effect on any saved/draft/published data
- [x] 2.7 Checkpoint: run `npm run typecheck`, `npm run build`, and `npm run test:e2e` and confirm all 12 existing flows still pass — one exception, confirmed with the user: `e2e/page-builder-editor.spec.ts` (from the prior change) asserted an explicit "Save" button, which the confirmed autosave design removes; updated that one test to assert the save-state indicator and Publish flow instead of clicking Save. All 12 flows pass with that single deliberate edit; no other existing test was touched.

## Phase 3 — Block library & canvas interactivity

- [x] 3.1 Build the categorized block library panel (Content: Hero; Referral mechanics: Leaderboard, Reward tiers, Countdown), each a tile with an icon and label, replacing the current type-picker `<select>` + button; opens from an "Add block" affordance in the sidebar
- [x] 3.2 Extend `PageRenderer` with an opt-in `editable?: boolean` prop (default `false`/omitted): when true, wrap each rendered section in a selection/hover frame; when false or omitted, rendering is unchanged from today (verify by confirming the public page and any other `editable`-omitted usage renders byte-identical output before/after this change)
- [x] 3.3 Add the floating hover/selection control strip on each canvas section (move up, move down, delete — no duplicate), wired to the same reorder/remove logic the sidebar controls use, with the entry-form section excluded from the delete control exactly as the sidebar excludes it
- [x] 3.4 Wire bidirectional selection: selecting a section in the sidebar highlights it on the canvas and reveals its edit form; selecting a section on the canvas highlights the matching sidebar entry and reveals its edit form
- [x] 3.5 Checkpoint: run `npm run typecheck`, `npm run build`, and `npm run test:e2e` and confirm all 12 existing flows still pass — the block library/canvas hover controls add a second "Move Reward tiers up"-style control (sidebar + canvas both offer reorder now, as designed), which made `e2e/page-builder-editor.spec.ts`'s existing locator ambiguous; scoped it to the sidebar's list specifically. All 12 flows pass; two transient full-suite runs failed on unrelated tests due to Prisma connection-pool exhaustion from this session's extensive manual testing (confirmed via server logs and by each affected test passing in isolation), resolved by restarting the dev server.

## Phase 4 — Full verification & rollout

- [x] 4.1 Add a new E2E test: editing a section (or the headline) and letting it autosave does NOT change the public page; confirm by loading `/c/[slug]` in a separate context before and after the edit and asserting no difference
- [x] 4.2 Add a new E2E test: pressing Publish after an edit does change the public page, reflecting the edited content
- [x] 4.3 Add a new E2E test: after an unpublished edit, pressing Discard reverts the editor to the previously published content, and the public page (already unaffected) remains unaffected
- [x] 4.4 Add a new E2E test: toggling the device preview changes the canvas's rendered width (assert via bounding box or viewport-affecting CSS) without any network write occurring and without changing the campaign's stored draft or published data
- [x] 4.5 Checkpoint: run `npm run typecheck`, `npm run build`, and the full `npm run test:e2e` suite and confirm all 16 flows (12 existing + 4 new) pass
- [ ] 4.6 Commit all changes from this feature and push to `main`
- [ ] 4.7 Confirm the Vercel deployment for this push succeeds, including the `vercel-build` script's `prisma migrate deploy` step applying the new migration to the production database; if it fails, fix the underlying cause and re-push
- [ ] 4.8 Directly verify the `pageConfigDraft` column actually exists in the production database (e.g., query `information_schema.columns` for `Campaign.pageConfigDraft`, or an equivalent direct check) — do not infer this from the Vercel build succeeding alone
- [ ] 4.9 Manually confirm `https://snowball-blue.vercel.app/c/launch-giveaway` renders correctly in production
